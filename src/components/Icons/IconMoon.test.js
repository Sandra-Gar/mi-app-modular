import React from 'react';
import { render } from '@testing-library/react';
import IconMoon from './IconMoon.js';

test('El icono de la luna se muestra correctamenteg', () => {
  render(<IconMoon />);
});
